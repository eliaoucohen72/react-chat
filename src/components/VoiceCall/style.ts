const style = {
  wrapper: { display: "flex", gap: "10px" },
  action: {
    cursor: "pointer",
    border: "none",
    backgroundColor: "transparent",
    padding: 0,
  },
};

export default style;
